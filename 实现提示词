你是资深量化交易架构师与全栈数据科学家，精通 Python、A 股市场、海龟交易法则和 Streamlit 可视化。

请构建一个完整的“海龟交易分析系统”，由两个文件组成：

1. turtle_core.py：核心分析逻辑
2. app.py：Streamlit 可视化界面与结论输出

系统要求如下：

【A. 核心分析逻辑 (turtle_core.py)】

- 函数 analyze_all_stocks(pool, entry_period, exit_period, atr_period, risk_pct, equity)
  返回一个 pandas DataFrame，包含所有股票的以下列：
    代码, 名称, 当前价, ATR, 突破信号(bool), 建议手数, 止损价,
    潜在突破位(20日最高价), 距离突破%(正数表示未突破，0或负表示已突破),
    最新日期, 加仓机会(bool), 加仓目标价列表
- 数据获取：用 akshare，仅分析 60 和 00 开头的股票；默认沪深300，可切换全市场。
- 策略参数放在函数参数中，外部可调。
- 必须处理异常，单只股票失败不中断。
- 海龟策略实现：
  入场 = 收盘价 > 过去 entry_period 日最高价
  出场 = 收盘价 < 过去 exit_period 日最低价 或 触发 2*ATR 止损
  头寸单位 = (equity * risk_pct) / ATR，向下取整到100股
  加仓 = 每0.5*ATR加一次，最多3次，记录目标价

【B. Streamlit 可视化界面 (app.py)】

- 页面标题“🐢 海龟交易分析看板”，侧边栏可调参数（股票池、通道周期、ATR周期、风险%、权益）。
- 点击“开始分析”后，主区域显示以下四个板块：
  1. 结论摘要（自动生成文字）
  2. 今日突破信号表
  3. 接近突破（距离<5%）表
  4. 全部股票表

- 结论摘要必须包含：
  * 总共分析股票数量
  * 今日触发买入信号的股票数量
  * 其中基本面估值合理的数量（PE<30且PB<3且ROE>10%），若有
  * 距离突破最近的前3只股票及距离百分比
  * 最重要的操作建议（例如：“今日市场趋势偏弱，建议观望”或“出现多个优质突破信号，可轻仓参与”），基于突破数量简单判断。

- 表格要带条件格式：突破信号 True 的行高亮，距离突破% 用颜色渐变。
- 单击任意股票行，下方展开该股票的详细分析卡片：
  * 显示股票名称、当前价、PE、PB、ROE（从实时行情获取）
  * 显示入场、止损、加仓目标价（数值）
  * 交互式 K 线图（用 plotly 绘制），包含：
    - 近 100 天的日K线（mplfinance 风格或 plotly 自建蜡烛图）
    - 唐奇安通道上轨/下轨线（entry_period 和 exit_period）
    - ATR 止损带（入场价-2*ATR 水平线，若已突破）
    - 用竖直虚线标注突破日期
  * 图表可缩放、可保存。

- 提供一键下载完整分析结果 CSV 的按钮。

【C. 技术与风格要求】

- 所有代码有详细中文注释，解释金融含义。
- 依赖库：akshare, pandas, numpy, streamlit, plotly（若绘图需其他库请自行添加并说明）。
- 处理实时行情中 PE/PB/ROE 可能为“-”或缺失的情况，显示为 N/A。
- 使用 @st.cache_data 缓存分析结果，避免重复计算（在 Streamlit 中点击分析时才更新）。
- 文件开头注明运行方式：在终端执行 streamlit run app.py

【D. 直接输出完整的两个文件内容，不要省略任何细节】